{
  "name": "api-server",
  "version": "1.0.0",
  "description": "To avoid exposing the openAI api key, creating a simple Node.js server that acts as a proxy for the API requests from extension",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Aishwary",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.3.4",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2"
  }
}
